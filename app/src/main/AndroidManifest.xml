<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".database.CajeroApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/mi_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mi_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bancoalbeca">
        <activity
            android:name=".AtmFormActivity"
            android:exported="false" />
        <activity
            android:name=".AtmListActivity"
            android:exported="false" />
        <activity
            android:name=".AtmManagementActivity"
            android:exported="false"
            android:label="@string/title_activity_atm" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".posicion.PosicionGlobalDetailActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".TransferActivity" />
        <activity android:name=".MovimientosActivity" />
        <activity android:name=".posicion.PosicionGlobalActivity" />
    </application>

</manifest>